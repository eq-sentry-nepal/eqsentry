# Keep runtime and local-only files out of the Docker image.
node_modules
.env
.env.*
!.env.example

# Runtime subscriber/report data and local backups must never be baked in.
data/
backups/

# Development-only files.
test/
*.log
