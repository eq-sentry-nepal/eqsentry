# Dependencies
node_modules/
server/node_modules/

# Secrets & runtime data
server/.env
.env
server/data/*.json

# OS / editor
.DS_Store
Thumbs.db
*.log

# Build output
dist/

# Local backups (daily scheduled task)
server/backups/
_backups/
_pre-pull-backup/

# Locally generated dependency-audit reports (weekly scheduled task, not CI).
# Delete this line if you'd rather keep the audit history in the repo.
docs/dep-audit-*.md
